DEFAULT SimulationMain

USESETUPVARS hybrid_precision

IF hybrid_precision
   LINKIF Flash_mpi.h.hybrid_precision Simulation
   # signals that FLASH will be complied with real = real*4
   PPDEFINE HYBRID_PRECISION
ENDIF
